```bash
# 安装 texlive-lang-chinese（包含 ctex）
sudo apt update
sudo apt install texlive-lang-chinese

# 安装 XeLaTeX 支持
sudo apt install texlive-xetex

# 安装中文字体
sudo apt install fonts-noto-cjk fonts-wqy-microhei
```